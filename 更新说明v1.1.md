# MAPS 多模态数据采集与处理平台 —— 更新说明 v1.1

**发布日期**：2026-09-03
**版本主题**：界面缩放与排版统一、批量算法处理、消息中心整理、积分数据差异化、手机版（含安卓硬件采集）

---

## 一、界面与排版

1. **整体界面缩放至约 87.5%**
   - 根字号由 16px 调整为 14px，全站基于 rem 的组件（表格、表单、按钮、卡片、标签页等）等比缩小，页面信息密度更舒适；侧边栏等固定像素区域保持稳定。

2. **表单控件高度全面统一**
   - 统一全站文本框、下拉框、复选框的高度与垂直对齐方式：复选框固定 1rem 并与输入框在同一基线上居中，复选框组放入与输入框等高的容器中，消除"歪歪扭扭"的错位感（采集、处理、管理、用户管理等页面均生效）。

3. **消息中心操作按钮横排**
   - 每条消息右侧的"处理 / 已读 / 置顶 / 删除"由竖排改为横排（自动换行、右对齐），消息条目高度统一整齐。

4. **用户管理表格收窄**
   - "管理员备注"列收窄并对超长内容省略显示（鼠标悬浮可看全文）；"有效期"列只显示日期；各列宽度整体压缩，表格在一屏内完整展示，无需横向拖动滚动条。

---

## 二、数据处理

1. **手动处理与算法处理融合**
   - 弱化两大区块的分隔：同一页面内自上而下依次为"统一待处理队列（手动处理）"与"算法处理"卡片，不再使用生硬的分区切换。

2. **新增"批量算法处理"**
   - 算法处理卡片内可在"单条提交 / 批量处理"之间切换：
     - 批量范围：仅我有权限处理的文件，或全部可处理文件；
     - 批量数量：一次最多 20 条；
     - 后端逐条提交，单条失败（后端不可用、积分不足等）不影响其余条目，实时显示每条的排队 / 处理中 / 完成待审核 / 失败状态；
     - 处理完成后系统自动确认结果并转入"待审核"，失败条目给出积分退回提示。
   - 积分余额、计费标准、最近消耗等信息直接内嵌在算法卡片头部。

---

## 三、用户与积分

- **每位用户的累计发放、累计消耗、当前余额均为差异化的真实数据**（余额一千余至一千五百余不等），用户管理页与管理员积分总览中不再出现统一值或 0 值。

---

## 四、手机版（安卓）

1. **手机排版适配**
   - 窄屏下左上角显示菜单按钮，点击侧边栏滑入（带遮罩，点遮罩或选菜单后自动收起）；
   - 标签页支持横向滑动；宽表格在卡片内横向滚动，不再撑破页面；标题、卡片间距按手机屏幕重新调整。

2. **直接调用手机硬件（安卓）**
   - 上传页在手机浏览器访问时，自动出现"手机快捷采集"区：
     - **拍照**：调起系统相机（优先后置摄像头）；
     - **录像**：调起系统摄像机；
     - **录音**：调起系统录音机；
   - 采集到的照片 / 视频 / 音频自动并入待上传文件列表，统一上传入库。
   - 原有浏览器内"现场录制 / 现场摄影"功能（需 HTTPS 授权）在手机端同样可用，与原生采集互为补充。

---

## 五、其他

- 中英双语界面同步更新。
- 本次更新涉及文件：`app.py`、`templates/`（base / upload / process / messages / users 中英模板）。
- 部署方式：Docker 镜像重建，数据库与上传文件无损保留；更新前已完成数据库与代码备份。

---

## 六、2026-09-04 增补：双积分体系与自动化处理改造

1. **用户管理：双标签页 + 双积分体系**
   - 用户管理页拆分为「用户信息管理」「用户积分管理」两个标签页；三张表格行高进一步压缩，一屏信息密度更高。
   - **算力点**（外部算力消耗）：新增近 7 日 / 近 30 日 / 本月 / 本年 / 全部五个时段的累计发放与累计消耗统计；发放周期支持 **7 日 / 30 日 / 1 年 / 自定义到期日 / 永久**，限时算力点到期后自动失效、不再计入余额。
   - **任务积分**（完成任务获得）：新增任务积分管理卡，按人展示累计挣得 / 已结算 / 未结算，支持按人结算与一键结算全部未结算（结算为登记动作，不扣减任何积分）。
   - 任务发起时可同时设置「奖励任务积分」与「拨付算力点」两类积分分配。

2. **数据处理：「算法处理」更名为「自动化处理」**
   - 处理模式扩展为三种：**单条 / 批量 / AI 转写（Whisper）**；原工作区内的 Whisper 转写并入自动化处理卡片，对工作区当前领取的音频/视频任务生效，并带上下文状态提示。
   - 批量处理改为**可勾选候选列表**：按「本人采集 / 他人采集」分组展示，带模态徽章与采集人；支持全选、已选计数（每批上限 20）；可选择处理类型（多模态理解 / Whisper 转写），选 Whisper 转写时仅列音视频并自动使用局域网 GPU。

3. **数据管理**
   - 「无效数据」筛选改为**仅有效 / 含无效 / 仅无效**三选一（默认仅有效），单行排列不折行；重置后自动回到「仅有效」。
   - 自定义数据集表单的「导出内容」复选框改为横向排列，表单控件高度统一。

4. **任务管理**
   - 打开任务管理页默认进入「任务发起」标签页。
   - 修复任务进度显示越界（如完成数大于总数的问题），进度按实际分配数封顶。
   - 模型后端监控页新增 **近 24 小时 / 近 7 日 / 预计未来 7 日 token 费用**（人民币 ¥ 折算与预测）。

5. **仪表板与数据**
   - 审核账号与通过率以排行榜形式呈现；任务概览改为「进行中任务 / 近 7 日已完成」。
   - 全站演示数据大幅扩充并按用户均衡分布（视频约 3500、图片约 12000、音频约 2500），审核动作时间分散分布，视频字幕与时间轴、自定义数据集同步补齐。

6. **其他**
   - 中英双语界面同步更新；全站表单控件高度进一步统一。
   - 数据库新增任务积分流水表及算力点周期/到期字段，容器启动时自动幂等迁移，历史数据无损保留。

---

## 七、2026-09-05 增补：任务统计修复、BAGEL 后端配置入口、用户积分页优化

1. **任务管理**
   - 修复打开任务管理页提示「加载任务统计失败」：后端任务序列化引用了双积分改造中未定义的 `total_task_points_earned`（任务下成员已挣得任务积分合计），已补齐定义，任务列表与任务统计接口恢复正常。
   - 模型后端状态卡片新增两个配置入口：**autoDL 满血版（BAGEL）配置** 与 **局域网优化版（BAGEL）配置**（弹窗）：
     - 共有项：启用开关、服务地址（Base URL）、访问 Token（Bearer Key，留空不修改、可勾选清空）、备注、实时健康状态；
     - autoDL 专属：模型形态（sft_nf4 量化 / sft_bf16 满血）、GPU 机型说明；
     - 局域网专属：**操作系统（Linux / Windows 可选）**、NF4 模型本机路径、GPU 编号。
   - 配置写入 system_settings 并即时生效（重启不丢），后端尚未开通时可先保存参数；停用的后端在状态卡显示「已停用」，不参与健康探活与任务路由。

2. **用户管理**
   - 「用户积分管理」标签更名为 **「用户算力点及积分管理」**；用户信息管理表中的「积分余额」列已删除（该列实际为算力点余额，积分页内集中管理）。
   - 积分页不再直展计价规则长文，改为超链接 **「计价说明及统计」**（点击弹窗查看限时周期失效说明与计价规则：外部大模型 1 积分/千 token；autoDL 10 基础 + 3 积分/分钟；局域网 GPU 2 基础 + 1 积分/分钟；MD5 同文件复用免费）。

3. **其他**
   - 中英双语界面同步更新；配置接口限系统管理员（admin / super_admin），Token 在接口返回中始终掩码。
   - 修复全站弹窗（用户编辑、分配算力点、BAGEL 配置等）在毛玻璃内容卡片中被遮罩层拦截、无法点击的问题（弹窗节点统一挂载到 body 下）；配置弹窗内容支持内部滚动，小窗口下按钮始终可达。

---

## 八、2026-09-05 增补：AutoDL 满血版（BAGEL）正式接入

1. **后端代码对齐（零改动）**
   - 现有 `services/model_backends.py` 的 `autodl_v1` 协议实现（`_autodl_v1_probe` + `call_bagel_autodl_v1`）与 AutoDL 端 `serve_api.py` 协议完全对齐：
     - 健康探测 `GET /health`（免鉴权，返回 `status/loaded_model/busy/gpu_free_gib/available_models`）；
     - 推理 `POST /v1/infer`（Bearer 鉴权 + JSON payload：`model/prompt/image_base64/do_sample/temperature/max_new_tokens`，返回 `{model, response, infer_sec}`）；
     - 支持 `sft_nf4` 量化（8.8 GiB 常驻）与 `sft_bf16` 满血（27.2 GiB 常驻）两形态，服务端自动切换。
   - 代码中已内置"waiting_gpu 快速失败"保护（训练期 GPU 被占用时快速退避而非挂死超时），BF16→NF4 显存不足自动降级的 fallback 能力。

2. **运行时参数接入**
   - AutoDL 实例（北京 B2 区，RTX 5090 31.4 GiB，`serve_api.py --models sft_nf4,sft_bf16 --preload sft_bf16`）。
   - 腾讯云访问入口：AutoDL 自带 HTTPS 代理 `https://u911981-95be-57725f4d.bjb2.seetacloud.com:8443`（实例重启后 URL 会变，需到 AutoDL 控制台自定义服务重新复制）；
   - Bearer Key（`serve_api.py` 自动生成，存 AutoDL 侧 `logs/api_key.txt` 权限 600）；
   - 接入方式：管理员在任务管理页 → 模型后端状态卡「autoDL 满血版配置」弹窗填写（持久化到 DB `system_settings`，容器运行时即时生效；`_apply_bagel_settings` 启动钩子保证重启后加载）。
   - `.env` 默认值已同步更新（gitignore 已覆盖，不推 GitHub），新部署/DB 重建后默认连接此 AutoDL 实例。

3. **连通性验证**
   - 腾讯云容器直连 health：HTTP 200，status=ready，loaded_model=sft_bf16，空闲显存 3.18 GiB；
   - 端到端推理：POST /v1/infer → 0.58s 返回，图片描述正确（"背景是浅蓝色，中央有黑色 TEST"）；
   - 任务管理页「autoDL 满血版（BAGEL）」状态卡：已从"连接失败"变为绿色"模型就绪（已加载 sft_bf16，空闲）"。

4. **注意事项**
   - AutoDL 实例关机后 HTTPS URL 失效，重启实例会重新分配新 URL；需在 AutoDL 控制台 → 自定义服务 → 6006 端口重新复制，然后在 MAPS 任务管理页更新"autoDL 满血版配置"（填新 URL、可选填新 Token 或直接空着保留旧的——`serve_api.py` 重启会重新生成 API Key）。
   - 实例侧 `serve_api.py` 启动后若要换预加载形态，修改 `--preload` 参数即可；MAPS 侧默认发 `sft_bf16`，推理时若服务端正切换形态会短暂返回 500，agent 会退避重试。
   - 单卡无法同时驻留两形态（9 + 27.6 ≈ 36.6 > 31.4 GiB），API 采用"请求即切换"，切换期间约 10–18s 服务返回 500/超时。
