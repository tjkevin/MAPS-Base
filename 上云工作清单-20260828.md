# MAPS 系统上云工作清单

> 创建时间：2026-08-28
> 项目路径：`d:\MAPS\`（已从 MMDAPS 更名为 MAPS）
> 说明：代码中所有 mmdaps 引用已更新为 maps。上云后按此清单逐项处理。

---

## 一、云服务器准备

### 1.1 服务器选购建议
- **最低配置**：2核4G（如腾讯云轻量应用服务器）
- **推荐配置**：4核8G或以上（多模态模型推理需要更多内存）
- **操作系统**：Ubuntu 22.04 LTS
- **系统盘**：40G+（建议挂载数据盘用于存储 uploads 和数据库）
- **带宽**：按实际用户量，最低5M峰值
- **安全组开放端口**：
  - 22（SSH）
  - 80（HTTP，Nginx）
  - 443（HTTPS，Nginx）
  - 5000（Flask，建议仅对内网开放或通过Nginx代理）
  - 3306（MySQL，**禁止对公网开放**）
  - 6379（Redis，**禁止对公网开放**）

### 1.2 服务器基础环境
```bash
# 安装Docker和Docker Compose（Ubuntu）
sudo apt update && sudo apt install -y docker.io docker-compose
sudo systemctl enable docker && sudo systemctl start docker

# 将当前用户加入docker组（免sudo）
sudo usermod -aG docker $USER
```

---

## 二、代码部署

### 2.1 上传代码到服务器
```bash
# 推荐：使用Git
cd /opt
git clone <你的仓库地址> maps
cd maps

# 或用scp上传压缩包（本地执行）
# 先在本地打包，排除不必要文件
cd D:\
tar -czf maps-deploy.tar.gz --exclude=.venv --exclude=__pycache__ --exclude=.git --exclude=uploads --exclude=*.db MAPS/
scp maps-deploy.tar.gz root@<服务器IP>:/opt/
# 服务器上解压
ssh root@<服务器IP>
cd /opt && tar -xzf maps-deploy.tar.gz && mv MAPS maps
```

### 2.2 创建生产环境配置文件
在项目根目录创建 `.env` 文件：
```bash
cat > .env << 'EOF'
# ========== MAPS 生产环境配置 ==========

# Flask安全密钥（必须修改！生成命令：python -c "import secrets; print(secrets.token_hex(32))"）
SECRET_KEY=<在此处替换为随机生成的密钥>

# ---------- 数据库配置 ----------
# Docker Compose 内置MySQL
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_USER=maps
MYSQL_PASSWORD=<修改为强密码>
MYSQL_DATABASE=maps

# 或使用云RDS（推荐生产环境）
# DATABASE_URL=mysql+pymysql://maps:<RDS密码>@<RDS内网地址>:3306/maps?charset=utf8mb4

# 连接池配置
MYSQL_POOL_SIZE=10
MYSQL_MAX_OVERFLOW=20

# ---------- Redis配置 ----------
REDIS_URL=redis://:<Redis密码>@redis:6379/0

# ---------- BAGEL配置 ----------
BAGEL_SERVICE_URL=http://<BAGEL服务地址>:8000
BAGEL_QUEUE_NAME=bagel:task:queue
BAGEL_RESULT_TTL_SECONDS=86400

# ---------- 会话配置 ----------
SESSION_IDLE_TIMEOUT=1800

# ---------- 时区 ----------
TZ=Asia/Shanghai
EOF
```

### 2.3 修改 docker-compose.yml 中的默认密码
**重要**：docker-compose.yml 中的以下默认密码必须修改为与 .env 一致：
- `MYSQL_ROOT_PASSWORD: maps_root_dev` → 改为强密码
- `MYSQL_PASSWORD: maps_dev` → 与.env中一致
- `--requirepass maps_redis_dev` → 与.env中Redis密码一致
- `SECRET_KEY` → 与.env中一致

---

## 三、启动服务

### 3.1 构建并启动
```bash
cd /opt/maps

# 构建镜像并启动核心服务（Web + MySQL + Redis）
docker-compose up -d --build

# 如需BAGEL异步Worker，额外启动：
docker-compose --profile worker up -d --build

# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f web
docker-compose logs -f mysql
```

### 3.2 验证服务
```bash
# 检查Web服务
curl http://localhost:5000/login

# 检查MySQL连接
docker-compose exec mysql mysql -u maps -p maps -e "SHOW TABLES;"

# 检查Redis
docker-compose exec redis redis-cli -a <Redis密码> ping
```

### 3.3 初始化数据
首次启动后，系统会自动 `db.create_all()` 创建表结构，并自动创建 `testuser/Test@pwd1` 用户。
**生产环境必须做的事**：
1. 登录后立即修改 testuser 密码
2. 在用户管理中创建实际的管理员账号，禁用或删除 testuser
3. 检查消息模板是否正常初始化

---

## 四、Nginx反向代理与HTTPS

### 4.1 安装Nginx
```bash
sudo apt install -y nginx
```

### 4.2 配置Nginx
创建 `/etc/nginx/conf.d/maps.conf`：
```nginx
server {
    listen 80;
    server_name your-domain.com;

    client_max_body_size 4096M;

    location /static/ {
        alias /opt/maps/uploads/;
        expires 30d;
    }

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 600s;
        proxy_send_timeout 600s;
        proxy_connect_timeout 60s;
    }
}
```

```bash
sudo nginx -t && sudo systemctl reload nginx
```

### 4.3 配置HTTPS（Let's Encrypt免费证书）
```bash
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

---

## 五、数据持久化与备份

### 5.1 数据卷说明
docker-compose.yml 已配置以下持久化卷：
- `maps_mysql_data`：MySQL数据文件
- `maps_redis_data`：Redis持久化文件
- `./uploads`：上传的多模态文件（挂载到宿主机）
- `./subtitles`：字幕文件（挂载到宿主机）

### 5.2 备份策略
```bash
# 创建备份脚本 /opt/maps/scripts/backup.sh
#!/bin/bash
BACKUP_DIR=/data/backups/maps
DATE=$(date +%Y%m%d_%H%M%S)
mkdir -p $BACKUP_DIR

# 备份MySQL
docker-compose exec -T mysql mysqldump -u maps -p<密码> maps | gzip > $BACKUP_DIR/db_$DATE.sql.gz

# 备份uploads
tar -czf $BACKUP_DIR/uploads_$DATE.tar.gz uploads/

# 保留最近30天备份
find $BACKUP_DIR -name "*.gz" -mtime +30 -delete
```

```bash
chmod +x /opt/maps/scripts/backup.sh
# 每天凌晨3点备份
(crontab -l 2>/dev/null; echo "0 3 * * * /opt/maps/scripts/backup.sh") | crontab -
```

---

## 六、安全加固

### 6.1 必须修改的默认凭据
- [ ] MySQL root密码
- [ ] MySQL maps用户密码
- [ ] Redis密码
- [ ] Flask SECRET_KEY
- [ ] testuser默认密码或删除该用户

### 6.2 防火墙/安全组
- [ ] 仅开放22、80、443端口
- [ ] 3306、6379、5000端口禁止公网访问
- [ ] SSH建议修改默认端口或使用密钥登录

### 6.3 应用层安全
- [ ] 确认密码复杂度策略符合要求
- [ ] 确认登录失败锁定策略参数
- [ ] 生产环境使用waitress（Docker已配置），非Flask debug
- [ ] HTTPS环境下配置SESSION_COOKIE_SECURE=True

---

## 七、BAGEL/多模态模型配置（按需）

- [ ] 确认BAGEL服务地址和端口可达（`BAGEL_SERVICE_URL`）
- [ ] 确认网络策略允许容器访问BAGEL服务
- [ ] 启动BAGEL Worker：`docker-compose --profile worker up -d`
- [ ] 测试算法处理功能是否正常
- [ ] 如需本地CLIP+Mamba/Whisper，确认服务器内存8G+

---

## 八、域名与DNS（可选）

- [ ] 购买/使用已有域名
- [ ] 添加A记录指向服务器公网IP
- [ ] 完成ICP备案（国内服务器必须）
- [ ] 配置Nginx和HTTPS

---

## 九、上线验证清单

- [ ] 网站可通过域名/IP正常访问
- [ ] 用户登录、登出正常
- [ ] 文件上传（视频/图片/音频）功能正常
- [ ] 浏览器录制功能正常（webm格式）
- [ ] B站视频抓取功能正常
- [ ] 数据处理（手动标注）功能正常
- [ ] 数据审核（通过/打回/自行处理）功能正常
- [ ] 数据管理（筛选、分页、预览、导出）功能正常
- [ ] 任务管理（创建、分配、申领、流程流转）功能正常
- [ ] 用户管理（创建、角色权限、禁用）功能正常
- [ ] 消息通知（站内信）功能正常
- [ ] 算法处理（BAGEL/Whisper）功能正常（如已配置）
- [ ] 数据集制作和导出功能正常
- [ ] 大文件上传（分片上传）功能正常
- [ ] 中英文切换正常

---

## 十、常用运维命令速查

```bash
# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f web
docker-compose logs -f --tail=100 web

# 重启服务
docker-compose restart web

# 重新构建并启动（代码更新后）
docker-compose up -d --build web

# 进入容器
docker-compose exec web bash
docker-compose exec mysql mysql -u maps -p maps

# 停止所有服务
docker-compose down

# 停止并删除数据卷（危险！会丢失数据）
docker-compose down -v

# 更新代码后重新部署
git pull
docker-compose up -d --build web
```

---

## 十一、容器命名对照

| 服务 | 容器名 | 说明 |
|------|--------|------|
| MySQL | maps-mysql | 数据库 |
| Redis | maps-redis | 缓存/队列 |
| Web | maps-web | Flask主应用 |
| Worker | maps-bagel-worker | BAGEL异步任务（可选） |
| 数据卷 | maps_mysql_data | MySQL持久化 |
| 数据卷 | maps_redis_data | Redis持久化 |
| 网络 | maps-network | Docker内部网络 |

---

## 十二、联系继续处理时需提供的信息

上云后联系我继续处理时，请提供：

1. **云服务器信息**：操作系统、公网IP、配置规格
2. **部署方式**：Docker Compose / 直接部署
3. **数据库**：内置MySQL / 云RDS
4. **Redis**：内置Redis / 云托管Redis
5. **BAGEL服务**：地址、是否已就绪
6. **域名**：是否已备案、是否配置HTTPS
7. **遇到的问题**：具体错误日志或截图
8. **docker-compose ps 输出**：各容器运行状态

---

*文档结束*
