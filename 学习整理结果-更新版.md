# MAPS (MMDAPS) 项目学习整理结果 - 更新版

> 更新说明：本版基于 2026-08-14 实际代码复核和运行验证更新，包含 Docker 容器化方案、.venv 环境配置修正、系统命名说明等。

---

## 〇、系统命名说明

**正式名称：MAPS (Multimodal Data Acquisition and Processing System)**

> 注：代码中仍保留部分 MMDAPS 标识（因重构工作量较大暂未全部替换），对外文档和系统界面使用 MAPS 名称。

---

## 一、项目概述

**MAPS** 是一个**多模态数据采集与处理系统**，主要用于音视频等多媒体数据的上传、处理、审核、管理和任务协作。系统采用 Flask Web 框架开发，支持中英文双语界面。

**BAGEL 集成说明**：系统通过 Redis 异步队列（`services/bagel_queue.py`）与字节跳动的 BAGEL 多模态大模型服务交互，用于图片/视频内容智能分析。`scripts/bagel_worker.py` 是消费队列并调用 BAGEL API 的 Worker 进程。

---

## 二、技术栈

### 后端技术
- **Web 框架**: Flask (Python)
- **ORM**: Flask-SQLAlchemy
- **用户认证**: Flask-Login
- **生产 WSGI 服务器**: waitress (Windows/Linux 跨平台)
- **数据库**: 
  - 默认: MySQL (utf8mb4，支持中文和 emoji)
  - 开发回退: SQLite
- **缓存/队列**: Redis (用于 BAGEL 异步算法任务队列)
- **密码安全**: Werkzeug 密码哈希

### 前端技术
- **模板引擎**: Jinja2
- **UI 框架**: Bootstrap
- **脚本**: 原生 JavaScript (fetch API, DOM 操作)
- **图表**: Chart.js
- **无独立前端构建**: 非 Vue/React 单页应用

### 多模态 AI 模块
- **CLIP** (OpenAI): 图像/文本特征提取 (openai/clip-vit-base-patch32)
- **Mamba**: 状态空间模型，用于视频帧序列长程时序建模（纯 PyTorch 实现，兼容 Windows/CPU）
- **Whisper**: OpenAI 语音识别 (ASR)
- **BAGEL**: 字节跳动多模态大模型服务（外部 API 调用）
- **PyTorch**: 深度学习框架（当前稳定版本 2.4.1 CPU版，Windows DLL 加载正常）
- **OpenCV**: 视频帧采样

### 音频处理
- librosa, pydub, soundfile
- 支持 mp3, wav, flac, m4a 等格式

### 容器化部署
- **Docker**: 多阶段构建 Dockerfile
- **Docker Compose**: 一键编排 Web + MySQL 8.0 + Redis 7 + BAGEL Worker

---

## 三、开发环境配置（2026-08-14 更新）

### Python 版本
- **推荐版本**: Python 3.12.x（独立安装版，非 Anaconda）
- 虚拟环境目录: **`.venv`**（Python 行业标准命名，VS Code/PyCharm/Poetry/Pipenv 自动识别）

### 虚拟环境创建与激活（PowerShell）
```powershell
# 创建虚拟环境（如已存在可跳过）
& "D:\Program Files\Python\Python312\python.exe" -m venv .venv

# 激活虚拟环境
.\.venv\Scripts\Activate.ps1

# 验证激活成功（应显示 .venv 路径下的 python）
where python
python --version  # Python 3.12.0

# 安装依赖
pip install -r docs/requirements.txt
```

### ⚠️ 已知环境问题
- 旧 `venv/` 目录（基于 Anaconda 3.11 创建）已损坏，因 Anaconda 路径 `D:\ProgramData\anaconda3\python.exe` 已失效
- **不要直接修改 pyvenv.cfg 跨版本修复 venv**，会导致二进制包 ABI 不兼容
- torch 2.5+ 和 2.13.0 在 Windows 上存在 DLL 初始化失败问题，锁定使用 **torch 2.4.1 CPU 版**

---

## 四、目录结构说明

```
d:\MMDAPS\
├── app.py                          # Flask 主应用，路由和 API 定义（3300+行，建议后续拆分为Blueprint）
├── config.py                       # 配置文件（数据库、Redis、上传等）
├── models.py                       # SQLAlchemy 数据库模型定义
├── asr_processor.py                # 语音识别处理模块
├── init_database.py                # 数据库初始化脚本（⚠️ 硬编码MySQL，SQLite需手动调用db.create_all()）
├── .env.example                    # 环境变量示例
├── .gitignore                      # Git忽略规则（已添加.venv/）
├── Dockerfile                      # 🆕 Docker多阶段构建配置
├── docker-compose.yml              # 🆕 Docker Compose编排文件
├── .dockerignore                   # 🆕 Docker构建忽略文件
│
├── .venv/                          # 🆕 Python虚拟环境（行业标准命名，不提交Git）
│
├── services/                       # 业务服务层
│   ├── auth_security.py            # 认证安全服务（密码策略、登录锁定）
│   ├── bagel_queue.py              # BAGEL异步任务队列（基于Redis LIST+HASH）
│   ├── data_management.py          # 数据管理服务（查询、导出、统计）
│   ├── messaging.py                # 消息通知服务（站内信、公告）
│   └── task_workflow.py            # 任务工作流服务（分配、状态流转）
│
├── multimodal_models/              # 多模态 AI 模型模块（CLIP+Mamba）
│   ├── config.py                   # 多模态配置
│   ├── clip_extractor.py           # CLIP 特征提取器
│   ├── mamba_block.py              # Mamba 序列块（纯 PyTorch 实现）
│   ├── fusion_model.py             # CLIP+Mamba 融合模型
│   ├── retrieval.py                # 多模态检索
│   └── requirements_mm.txt         # 多模态依赖
│
├── templates/                      # Jinja2 HTML 模板
│   ├── base.html / base_cn.html / base_en.html
│   ├── index*.html                 # 首页/仪表板
│   ├── login*.html                 # 登录页
│   ├── upload*.html                # 数据上传
│   ├── process*.html               # 数据处理
│   ├── audit*.html                 # 数据审核
│   ├── manage*.html                # 数据管理
│   ├── tasks*.html                 # 任务管理
│   ├── messages*.html              # 消息中心
│   ├── users*.html                 # 用户管理
│   └── profile*.html               # 个人中心
│
├── scripts/                        # 工具脚本
│   ├── bilibili_crawler.py         # B站视频爬虫
│   ├── bagel_worker.py             # BAGEL异步任务Worker（消费Redis队列）
│   ├── batch_asr.py                # 批量语音识别
│   ├── import_videos.py            # 视频导入脚本
│   ├── setup_database.py           # 数据库设置
│   └── ...
│
├── docs/                           # 项目文档
│   ├── ARCHITECTURE_LAYERS.md      # 分层架构说明
│   ├── FUNCTIONAL_MODULES.md       # 功能模块划分
│   ├── DATABASE_TABLES_BY_CATEGORY.md  # 数据库表分类
│   ├── requirements.txt            # Python依赖（已更新为3.12兼容版本）
│   └── ...
│
├── instance/                       # 实例数据
│   └── mmdaps.db                   # SQLite数据库文件（旧路径，当前使用根目录mmdaps_local.db）
│
├── uploads/                        # 用户上传文件存储（Docker中作为Volume挂载）
├── subtitles/                      # 字幕文件存储（Docker中作为Volume挂载）
├── mmdaps_local.db                 # SQLite数据库文件（开发回退，根目录）
│
└── __pycache__/                    # Python缓存
```

---

## 五、核心业务流程

### 数据生命周期

```
上传/采集 → 数据处理 → 待审核 → 审核 → 数据管理/归档
   ↓           ↓         ↓       ↓         ↓
upload    process   pending_review  audit    manage
crawl     bagel     (待审核队列)   pass/     export
record    BAGEL_API (人工/算法)    reject    dataset
```

### 用户角色与权限

| 角色 | 权限说明 |
|------|---------|
| `super_admin` | 超级管理员，全部权限 |
| `admin` | 管理员，用户管理、系统配置 |
| `inspector` | 审核员，数据审核权限 |
| `recorder` | 数据采集/处理人员 |

### 数据状态流转

| 状态 | 说明 |
|------|------|
| `pending` | 待处理 |
| `processing` | 处理中 |
| `pending_review` | 待审核 |
| `pending_fix` | 待修正（审核打回，仅原处理人或管理员可领取） |
| `approved` | 审核通过（已归档） |
| `completed` | 已完成（旧版兼容） |
| `rejected` | 已驳回（旧版兼容） |

---

## 六、功能模块详解

### 1. 门户与总览（Dashboard）
- 路由: `/`
- 功能: 仪表板统计、待处理任务数、系统概览、5分钟自动刷新

### 2. 认证与个人中心
- 路由: `/login`, `/logout`, `/profile`
- 功能: 
  - 用户登录/登出
  - 密码强度验证（长度≥8，大小写+数字+特殊字符`!@#$%^&*-_=+`）
  - 登录失败锁定（5次失败后锁定30分钟）
  - 会话超时自动退出（默认30分钟）
  - 首次登录强制修改密码
  - 管理员可强制下线（session_version机制）

### 3. 数据采集（Upload/Crawl/Record）
- 路由: `/upload`, `/api/upload/*`, `/api/crawl/*`
- 功能:
  - **支持格式（17种）**:
    - 视频: mp4, avi, mov, mkv, webm, flv
    - 音频: mp3, wav, flac, m4a
    - 图片: jpg, jpeg, png, webp, gif
  - 文件大小限制: 4GB
  - 分片上传（5MB/片，断点续传）
  - 浏览器实时录制: **固定输出 webm 格式**（MediaRecorder API），文件名 `recording_{timestamp}.webm`
  - B站视频爬虫（当前仅支持bilibili，非通用视频网站）
  - MD5去重，自动生成采集元数据

### 4. 数据处理（Process）
- 路由: `/process`, `/api/process/*`
- 功能:
  - 手动转写/描述（支持时间轴）
  - BAGEL算法异步处理（Redis队列 + Worker进程）
  - 按模态（audio/video）分配任务
  - 打回修正后仅原处理人或管理员可领取

### 5. 数据审核（Audit）
- 路由: `/audit`, `/api/audit/*`
- 功能:
  - 待审核队列（按时间排序）
  - 审核通过（标记合规并归档）
  - 审核打回（附具体意见，通知原处理人）
  - 审核员自行修正（直接修改并通过）
  - 审核日志全留痕
  - 合规性检查（文件可读、元数据完整、MD5校验）
  - 音视频倍速播放（0.5x/1x/1.5x/2x）

### 6. 数据管理（Manage）
- 路由: `/manage`, `/api/manage/*`
- 功能:
  - 多条件筛选、分页
  - 预览媒体文件
  - 批量下载（ZIP）
  - 数据导出（多种格式）
  - 自定义数据集管理
  - 导出审计日志
  - 筛选模板保存

### 7. 任务与工作流（Tasks）
- 路由: `/tasks`, `/api/tasks/*`
- 功能:
  - 任务创建、发布、暂停、终止
  - 三种分配模式: 手动指派/自动分配（按当前任务负载均衡）/公海池
  - 任务领取、提交、复核、归档
  - 任务配额管理
  - 退领申请审批
  - 任务质量评分
  - 全生命周期操作日志

### 8. 消息与通知（Messages）
- 路由: `/messages`, `/api/messages/*`
- 功能:
  - 统一站内信收件箱（已完整实现）
  - 消息分类: 任务/审核/账号/系统
  - 优先级: 紧急/高/中/低
  - 已读/未读、置顶、删除
  - 消息模板配置
  - 系统公告发布（支持有效期、接收范围）
  - 消息投递日志审计
  - ⚠️ 邮件/短信渠道: 数据库有开关字段但发送功能未实现（预留扩展桩）

### 9. 用户管理（Users）
- 路由: `/users`, `/api/users/*`
- 功能:
  - 用户CRUD
  - 角色分配
  - 账号锁定/解锁
  - 账号有效期设置
  - 部门、联系方式管理

### 10. 安全审计
- 登录日志（成功/失败/超时/强制下线）
- 用户操作审计日志
- 消息投递日志
- 数据导出日志
- 审核操作日志
- 任务工作流日志

---

## 七、数据库模型

### 核心表结构（23张表）

| 表名 | 说明 |
|------|------|
| `users` | 用户表 |
| `login_logs` | 登录日志 |
| `user_audit_logs` | 用户操作审计 |
| `recordings` | 录制/媒体文件主表 |
| `acquisition_metadata` | 采集元数据（MD5、文件信息、来源） |
| `inspections` | 检查记录（旧版） |
| `processing_results` | 处理结果（文本、时间轴、算法原始输出） |
| `audit_logs` | 审核操作日志 |
| `audit_messages` | 审核消息（旧版） |
| `tasks` | 任务主表 |
| `task_assignments` | 任务分配 |
| `task_workflow_logs` | 任务工作流日志 |
| `task_return_requests` | 任务退领申请 |
| `task_messages` | 任务消息（旧版） |
| `inbox_messages` | 统一站内信 |
| `message_delivery_logs` | 消息投递日志 |
| `message_templates` | 消息模板 |
| `system_announcements` | 系统公告 |
| `message_channel_config` | 消息渠道配置（预留） |
| `filter_templates` | 筛选模板 |
| `data_sets` | 自定义数据集 |
| `data_set_items` | 数据集条目 |
| `export_logs` | 导出日志 |

### 关键关系
- User 1:N Recording（上传/处理）
- Recording 1:1 AcquisitionMetadata
- Recording 1:N ProcessingResult
- Recording 1:N AuditLog
- Task 1:N TaskAssignment
- Task 1:N TaskWorkflowLog
- User 1:N InboxMessage
- DataSet 1:N DataSetItem
- DataSetItem N:1 Recording

---

## 八、分层架构

```
┌─────────────────────────────────────────────────────────┐
│ ⑦ 运行与部署环境: waitress/gunicorn + Docker + Nginx    │
├─────────────────────────────────────────────────────────┤
│ ① 用户界面层: HTML5 + Bootstrap + 原生JS + Chart.js     │
├─────────────────────────────────────────────────────────┤
│ ② 表示层: Flask + Jinja2 模板 (中英双语)                │
├─────────────────────────────────────────────────────────┤
│ ③ 应用与业务逻辑层: Flask 路由/API + services/ 模块     │
│  ├─ auth_security  ├─ task_workflow                     │
│  ├─ data_management ├─ messaging                        │
│  └─ bagel_queue (Redis) → BAGEL大模型Worker             │
├─────────────────────────────────────────────────────────┤
│ ④ 持久化抽象层: Flask-SQLAlchemy ORM (models.py)        │
├─────────────────────────────────────────────────────────┤
│ ⑤ 数据与基础设施层: MySQL(主) / SQLite(开发) / Redis    │
├─────────────────────────────────────────────────────────┤
│ ⑥ 多模态AI层: CLIP + Mamba + Whisper + BAGEL API        │
└─────────────────────────────────────────────────────────┘
```

---

## 九、多模态 AI 模块详解

### CLIP + Mamba 融合架构

1. **CLIP 特征提取**
   - 图像/视频帧: `encode_images()` / `encode_video_frames()`
   - 文本: `encode_text()`
   - 模型: `openai/clip-vit-base-patch32`（可配置）

2. **Mamba 时序建模**
   - 纯 PyTorch 实现: `MambaBlockPyTorch`（Windows/CPU 兼容）
   - 可选官方 CUDA 实现: `mamba-ssm`（Linux/CUDA 环境）
   - 作用: 对视频帧序列做长程依赖建模

3. **Whisper 语音识别**
   - OpenAI Whisper ASR
   - 支持多种语言语音转文字
   - 通过 `asr_processor.py` 集成

4. **BAGEL 大模型集成**
   - 字节跳动多模态大模型服务（外部API）
   - 异步处理流程：Web API → Redis队列 → bagel_worker.py → BAGEL API → 结果写回Redis
   - Worker可独立部署/水平扩展

5. **融合与检索**
   - `CLIPMambaFusion`: 输出统一视频向量
   - `MultimodalPipeline`: 端到端管线
   - 支持: 视频-文本检索、图文检索

### 视频处理流程
1. 使用 OpenCV 按指定 FPS 采样视频帧（默认 1fps，最多 32 帧）
2. CLIP 编码每帧得到帧特征序列
3. Mamba 块对帧序列进行时序建模
4. 融合得到全局视频向量
5. 与文本向量计算余弦相似度进行检索

---

## 十、配置说明

### 环境变量（.env）

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `SECRET_KEY` | Flask 会话密钥 | 开发默认值（生产必须修改） |
| `DATABASE_URL` | 数据库连接 URL（最高优先级） | - |
| `MYSQL_HOST` | MySQL 主机 | 127.0.0.1 |
| `MYSQL_PORT` | MySQL 端口 | 3306 |
| `MYSQL_USER` | MySQL 用户 | mmdaps |
| `MYSQL_PASSWORD` | MySQL 密码 | mmdaps_dev |
| `MYSQL_DATABASE` | MySQL 数据库名 | mmdaps |
| `MMDAPS_USE_SQLITE` | 设为 1/true 使用 SQLite | - |
| `REDIS_URL` | Redis 连接 | redis://localhost:6379/0 |
| `SESSION_IDLE_TIMEOUT` | 会话空闲超时（秒） | 1800 |
| `BAGEL_SERVICE_URL` | BAGEL服务地址 | http://bagel-service:8000 |

### 上传配置
- 上传目录: `./uploads/`
- 最大文件大小: 4GB
- 分片大小: 5MB
- 允许扩展名: mp4, avi, mov, mkv, webm, flv, mp3, wav, flac, m4a, jpg, jpeg, png, webp, gif（共17种）

---

## 十一、快速启动

### 方式一：使用 SQLite（快速体验，已验证可运行）

```powershell
# 1. 激活虚拟环境
.\.venv\Scripts\Activate.ps1

# 2. 设置使用SQLite（当前目录已初始化好mmdaps_local.db，可跳过初始化）
$env:MMDAPS_USE_SQLITE='1'

# 3. 如需重新初始化数据库（可选）
# python -c "from app import app, db; \
#   from models import User; \
#   from werkzeug.security import generate_password_hash; \
#   app.app_context().push(); db.create_all(); \
#   admin = User.query.filter_by(username='admin').first() or \
#     User(username='admin', email='T1T2c@PjXkDvE.47v', \
#       password_hash=generate_password_hash('admin123'), \
#       role='super_admin', full_name='系统管理员', is_active=True); \
#   db.session.add(admin); db.session.commit()"

# 4. 启动应用
python app.py

# 5. 访问 http://127.0.0.1:5000
# 默认账号: admin / admin123
```

### 方式二：使用 MySQL（生产/完整功能）

```bash
# 1. 确保 MySQL 运行，创建数据库和用户
# CREATE DATABASE mmdaps CHARACTER SET utf8mb4;
# CREATE USER 'mmdaps'@'%' IDENTIFIED BY 'mmdaps_dev';
# GRANT ALL PRIVILEGES ON mmdaps.* TO 'mmdaps'@'%';

# 2. 复制并配置 .env
copy .env.example .env
# 编辑 .env 中的数据库配置

# 3. 安装依赖
pip install -r docs/requirements.txt

# 4. 初始化数据库（注意：init_database.py硬编码了MySQL root/password，可能需要修改）
python init_database.py

# 5. 启动 Redis（如需 BAGEL 异步队列）

# 6. 启动应用
python app.py
```

### 方式三：使用 Docker Compose（上云/一键部署，🆕）

```bash
# 启动Web+MySQL+Redis
docker-compose up -d

# 启动包含BAGEL Worker
docker-compose --profile worker up -d

# 查看服务状态
docker-compose ps

# 访问 http://localhost:5000
# 默认账号: admin / admin123

# 停止
docker-compose down
```

Docker 部署要点：
- 数据持久化：uploads/、subtitles/、mysql_data、redis_data 均通过 Volume 挂载
- 生产部署建议：使用云托管RDS代替容器MySQL，配置Nginx反向代理+HTTPS
- 修改默认密码（MySQL mmdaps_dev、Redis mmdaps_redis_dev、SECRET_KEY）

---

## 十二、Docker 容器化方案详解（🆕）

### 已创建文件

| 文件 | 说明 |
|------|------|
| `Dockerfile` | 多阶段构建：builder阶段安装编译依赖，最终阶段使用python:3.11-slim，内置waitress和健康检查 |
| `docker-compose.yml` | 4个服务：MySQL 8.0、Redis 7、Web(端口5000)、BAGEL Worker(可选profile) |
| `.dockerignore` | 排除venv、__pycache__、uploads、本地db等 |

### 注意事项
- BAGEL Worker需要能访问字节BAGEL API（公网或内网）
- 多模态AI模型较大，Docker镜像构建时会安装torch等大包
- 生产环境建议将MySQL/Redis替换为云托管服务

---

## 十三、代码优化空间建议

### 高优先级
1. **app.py 拆分**（3300+行过大）：按模块拆分为Flask Blueprint
2. **统一错误处理**：添加全局错误处理器和结构化logging
3. **文件上传安全**：增加文件魔数校验，不仅检查扩展名
4. **API响应格式统一**：当前返回格式不一致
5. **添加请求速率限制**（flask-limiter）防止暴力破解

### 中优先级
6. **init_database.py**：修复硬编码MySQL问题，支持SQLite初始化
7. **配置管理**：部分硬编码值改为环境变量
8. **前端**：大列表虚拟滚动、上传进度条
9. **BAGEL Worker**：添加重试机制、超时处理、进度回调
10. **数据库索引优化**：根据慢查询添加复合索引

### 低优先级
11. 修复DeprecationWarning（datetime.utcnow()、User.query.get()）
12. 添加pytest单元测试
13. 添加健康检查端点（/healthz）
14. 前端可考虑引入Alpine.js提升交互体验

---

## 十四、运行验证结果（2026-08-14）

### ✅ 已验证可正常运行的功能

实际启动应用并HTTP请求测试，以下功能全部返回200：

| 页面/API | 状态 |
|----------|------|
| 登录页 /login | ✅ 200 |
| 未登录访问首页 → 重定向/login | ✅ 302 |
| 登录功能（admin/admin123） | ✅ 成功 |
| 仪表盘 / | ✅ 200 |
| 数据上传 /upload | ✅ 200 |
| 数据处理 /process | ✅ 200 |
| 数据审核 /audit | ✅ 200 |
| 数据管理 /manage | ✅ 200 |
| 任务管理 /tasks | ✅ 200 |
| 用户管理 /users | ✅ 200 |
| 消息中心 /messages | ✅ 200 |
| 个人中心 /profile | ✅ 200 |
| 仪表盘API /api/dashboard/stats | ✅ 200 |
| 数据列表API /api/manage/data | ✅ 200 |
| 消息收件箱API /api/messages/inbox | ✅ 200 |
| 用户选项API /api/manage/users-options | ✅ 200 |
| 登出 /logout | ✅ 302 |

### ⚠️ 已知非关键问题（不影响运行）
1. `datetime.datetime.utcnow()` DeprecationWarning（app.py:3292）
2. `User.query.get()` LegacyAPIWarning（app.py:188）
3. init_database.py 不支持SQLite初始化
4. 邮件/短信发送功能未实现（仅开关字段）
5. 浏览器录制固定webm格式，不支持格式选择
6. 虚拟滚动功能未实现，使用传统分页

---

## 十五、项目特点与设计亮点

1. **完整的业务闭环**: 从数据采集→处理→审核→管理→归档，覆盖多媒体数据全生命周期
2. **完善的权限体系**: 4 种角色，细粒度权限控制
3. **全链路审计**: 登录、操作、审核、导出、消息投递均有日志留痕
4. **中英文双语**: 所有页面支持中英文切换
5. **灵活的任务工作流**: 支持多种分配模式，公海池、退领、质量评分
6. **统一消息中心**: 整合任务、审核、账号、系统消息，支持模板、公告
7. **多模态 AI 扩展**: 独立的 CLIP+Mamba+Whisper 模块，BAGEL大模型异步集成
8. **异步任务队列**: 基于 Redis 的 BAGEL 队列处理耗时算法任务，Worker可独立扩容
9. **数据库兼容**: 默认 MySQL，开发可回退 SQLite
10. **安全机制**: 密码强度校验、登录锁定、会话超时、强制下线、MD5 校验
11. **容器化支持**: Dockerfile + docker-compose.yml，一键上云部署
12. **生产就绪**: waitress跨平台WSGI服务器，健康检查，数据卷持久化

---

## 十六、关键文件索引

| 文件 | 重要性 | 说明 |
|------|--------|------|
| [app.py](file:///d:/MMDAPS/app.py) | ⭐⭐⭐⭐⭐ | Flask 主应用，所有路由和API（3300+行） |
| [config.py](file:///d:/MMDAPS/config.py) | ⭐⭐⭐⭐⭐ | 配置文件，数据库、Redis、上传配置 |
| [models.py](file:///d:/MMDAPS/models.py) | ⭐⭐⭐⭐⭐ | 所有数据库模型定义（23张表） |
| [services/task_workflow.py](file:///d:/MMDAPS/services/task_workflow.py) | ⭐⭐⭐⭐ | 任务工作流核心逻辑 |
| [services/data_management.py](file:///d:/MMDAPS/services/data_management.py) | ⭐⭐⭐⭐ | 数据管理查询导出 |
| [services/messaging.py](file:///d:/MMDAPS/services/messaging.py) | ⭐⭐⭐⭐ | 消息通知服务 |
| [services/auth_security.py](file:///d:/MMDAPS/services/auth_security.py) | ⭐⭐⭐ | 认证安全策略 |
| [services/bagel_queue.py](file:///d:/MMDAPS/services/bagel_queue.py) | ⭐⭐⭐ | Redis BAGEL异步队列 |
| [scripts/bagel_worker.py](file:///d:/MMDAPS/scripts/bagel_worker.py) | ⭐⭐⭐ | BAGEL Worker进程 |
| [multimodal_models/fusion_model.py](file:///d:/MMDAPS/multimodal_models/fusion_model.py) | ⭐⭐⭐ | CLIP+Mamba融合模型 |
| [Dockerfile](file:///d:/MMDAPS/Dockerfile) | ⭐⭐⭐ | Docker构建配置 |
| [docker-compose.yml](file:///d:/MMDAPS/docker-compose.yml) | ⭐⭐⭐⭐ | Docker Compose编排 |
| [docs/requirements.txt](file:///d:/MMDAPS/docs/requirements.txt) | ⭐⭐⭐⭐ | Python依赖（Python 3.12兼容） |

---

## 十七、学习总结

MAPS(MMDAPS) 是一个设计完善、功能完整的**多模态数据协作处理平台**，适合用于：
- 音视频数据采集标注团队协作
- 多媒体内容审核工作流
- 需要人工+AI 结合处理的多媒体数据场景
- 有审计留痕和权限管理需求的企业级应用

代码组织清晰，采用经典的 Flask 分层架构，业务逻辑拆分到 services 层，模型集中管理，模板支持国际化，支持Docker容器化部署，是学习 Flask Web 开发和多模态 AI 应用结合的优秀项目。

代码已在 Python 3.12 + SQLite 环境下实际运行验证，所有核心页面和API均可正常访问。
